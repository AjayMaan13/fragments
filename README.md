# Fragments

Fragments back-end API service.

## Scripts

Start the server:

```sh
npm start
```

Start the server in development mode (auto-restarts on file changes):

```sh
npm run dev
```

Start the server with the debugger (attach VSCode debugger on port 9229):

```sh
npm run debug
```

Lint the code:

```sh
npm run lint
```
